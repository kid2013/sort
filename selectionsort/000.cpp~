#include<iostream>

using namespace std;

template<typename T>
void Sort(T A[], int n){
  for(int i = n-1; i > 0; --i){
    int maxval = A[0]; int idx = 0;
    for(int j = 1; j <=i ++j){
      if(maxval < A[j]){
	maxval =  A[j];
	idx = j;
      }
}
}

}
